name: Update Driver Standings Debug

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-driver-standings-debug:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run driver standings script
        run: node updateStandings.js

      - name: Show generated JSON
        if: always()
        run: |
          echo "----- f1_driver_standings.json -----"
          if [ -f f1_driver_standings.json ]; then
            cat f1_driver_standings.json
          else
            echo "f1_driver_standings.json not found"
          fi

      - name: Commit updated file
        if: success()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add f1_driver_standings.json

          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git commit -m "Update driver standings debug run"
          git push